{
  "name": "taglib3",
  "version": "3.0.11",
  "description": "taglib bindings",
  "license": "LGPL-2.1",
  "repository": {
    "type": "git",
    "url": "git@github.com:schneefux/node-taglib3.git"
  },
  "scripts": {
    "test": "tape tests",
    "install": "prebuild-install --verbose || node build.js",
    "rebuild": "node build.js",
    "prebuild": "prebuild --force --strip --verbose --backend cmake-js"
  },
  "dependencies": {
    "async-lock": "^1.2.2",
    "cmake-js": "^6.1.0",
    "cross-spawn": "^7.0.2",
    "nan": "^2.14.0",
    "npm-run-path": "^4.0.1",
    "prebuild-install": "^5.3.3"
  },
  "devDependencies": {
    "prebuild": "^10.0.0",
    "tape": "*"
  }
}
